# BMS 智能楼宇暖通与能耗控制平台

平台对办公楼宇的暖通设备做时序计划控制：房间绑定温度设定与运行计划，
控制中心按时序计划下发设备指令并接收执行回执，能耗表周期采集并按房间
计量，超阈值触发告警，所有下发与计量事件写入审计。数据全部落本地文件，
控制台提供房间、计划、计量、审计四个页面。

## 构建与运行

```bash
go build -mod=vendor -o bin/bms ./cmd/bms
./bin/bms
```

默认监听 :18080，数据目录 ./data，首次启动自动写入演示数据。
访问 http://localhost:18080/rooms 打开控制台。

## 环境变量

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| BMS_ADDR | :18080 | HTTP 监听地址 |
| BMS_DATA | ./data | 持久化数据目录 |
| BMS_SEED | true | 首次启动是否写入演示数据 |
